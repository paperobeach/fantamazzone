# Usato solo da "npm run build" (produzione su Altervista).
# In sviluppo locale (npm run dev) NON viene letto: si usa invece
# il proxy '/api' -> http://localhost/legafanta definito in vite.config.js.
VITE_API_URL=/php_API
